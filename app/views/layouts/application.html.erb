<!DOCTYPE html>
<html>
<head>
  <title>Newmailboxer</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<% if signed_in? %>
	<%= "Welcome to mailboxer " + current_user.email %>
	&nbsp; &nbsp;
	<%= link_to 'Sign_out', :destroy_user_session, method: :delete %>
  <%= link_to 'Edit profile', :edit_user_registration %>
	<% else %>
    <p>Please, <%= link_to 'sign in', :new_user_session %></p>
    <p>or, <%= link_to 'sign up', :new_user_registration %></p>
  
	<% end %>
  <p><%= link_to 'Home', :root %></p>
<body>

<%= yield %>

</body>
</html>
