<%= link_to 'Move to trash', trash_conversation_path(@conv.id), :method => :post %>
<p> <b> Chat participants -> </b>
	<% @conv.recipients.each do |rec| %>
	<%= rec.email %>
	<% end %>

<p>

<b> <u> Conversation Subject</u> - </b>
<%= @conv.subject %>

<p>	Started by <b> <%= @conv.originator.email %> </b> 
	<%#= @creationDate %> 
	<%= time_ago_in_words(@conv.created_at) %> ago

<p>

<% @notif.each do |n| %>

<p> 
	<%= image_tag User.find(n.sender_id).user_image(:thumb) %>
	<b><%= User.find(n.sender_id).email %> </b>
			<p>
			<%= n.body %> &nbsp; &nbsp; &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
			<%= time_ago_in_words(n.updated_at) %> ago
			<%#= date = createDateTime(n.updated_at) %>

	<% end %>

<%= render 'formMessage' %>

