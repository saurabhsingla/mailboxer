<%= form_for :message, url:[:reply, conversation] do |f| %>
	<p>
	<%#= f.label :subject %>
	<%#= f.text_field :subject %>
	<p>
	<%= f.text_area :body, :cols=>28, :rows=>3, :placeholder=>'Type your message here' %>
	<p>
	<%= f.submit 'Reply' %>

<% end %>